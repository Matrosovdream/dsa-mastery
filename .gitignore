# Binaries
bin/
*.exe
*.dll
*.so
*.dylib
# Test binary, built with `go test -c`
*.test
# Output of `go tool cover` / profiles
*.out
coverage.*
*.prof
*.pprof
# Fuzz corpus caches (keep testdata/fuzz, drop the cache)
**/testdata/fuzz/**/cache/
# Go workspace file
go.work
go.work.sum
# Env / secrets
.env
*.local
# Editor / OS
.DS_Store
.idea/
# Scratch
/tmp
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
.gitignore
